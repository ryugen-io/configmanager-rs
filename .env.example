# Config Manager Environment Variables
# Copy this file to .env and customize for your setup

# Server host for display URLs
# Default: localhost
# Set to your machine's IP for network access (e.g., 10.1.1.30)
SERVER_HOST=localhost

# Server port (optional, defaults to 3000 in scripts)
# SERVER_PORT=3000
